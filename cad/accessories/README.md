# Accessories

FreeCAD sources for additional hardware that is not tied to one specific
machine. Same layout and export conventions as a machine folder - see
[`cad/README.md`](../README.md).

Empty for now; parts land here over time. The build packages this folder as
its own unit to `dist/accessories.zip` (skipped while there is nothing to
export).
